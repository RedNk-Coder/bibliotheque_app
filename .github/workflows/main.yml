name: Build APK with Buildozer

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      # --- Cloner le dépôt ---
      - name: Checkout repository
        uses: actions/checkout@v3

      # --- Installer Python ---
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.13

      # --- Installer dépendances système ---
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-virtualenv zip unzip openjdk-17-jdk build-essential git wget curl

      # --- Configurer Buildozer et venv ---
      - name: Setup Buildozer
        run: |
          python3 -m venv .venv
          source .venv/bin/activate
          pip install --upgrade pip
          pip install buildozer cython

      # --- Installer Android SDK & NDK ---
      - name: Install Android SDK & NDK
        run: |
          ANDROID_HOME="$HOME/android-sdk"
          mkdir -p $ANDROID_HOME/cmdline-tools
          cd $ANDROID_HOME/cmdline-tools
          wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O cmdline-tools.zip
          unzip cmdline-tools.zip
          mv cmdline-tools latest
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --sdk_root=$ANDROID_HOME --licenses
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --sdk_root=$ANDROID_HOME "platforms;android-33" "build-tools;33.0.2" "ndk;25.2.9519653"

      # --- Build APK ---
      - name: Build APK
        run: |
          source .venv/bin/activate
          buildozer android clean
          buildozer android debug

      # --- Upload APK ---
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: BibliothequePro-APK
          path: bin/*.apk
